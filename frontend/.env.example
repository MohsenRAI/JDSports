# Backend API Configuration
NEXT_PUBLIC_API_URL=http://127.0.0.1:5003

# Note: Using NEXT_PUBLIC_ prefix so the variable is available in the browser
# This replaces VITE_API_URL which is for Vite projects
